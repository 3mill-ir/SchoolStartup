@model IEnumerable<SchoolService.Models.AndroidJsonModel.TashvighTanbih_Model>
<div class="col-lg-12 col-md-12">
    <div class="col-lg-12 col-md-12" id="PipoBox2_Loading" style="text-align:center;padding-top:20px;display:none">
        <img src="~/Content/AdminContent/z3mill/ajax-load.gif" />
    </div>
    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-hover" id="sample-table-4">
            <thead>
                <tr>
                    <th class="center">تشویق / تنبیه</th>
                    <th class="center">تاریخ</th>
                    <th class="center">توضیحات</th>
                    <th class="center">امتیاز</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        @if (int.Parse(item.Emtiaz) > 0)
                        {
                            <td class="center"><img src="~/Content/AdminContent/z3mill/Tik.png" style="height:25px;width:25px;" /></td>
                        }
                        else
                        {
                            <td class="center"><img src="~/Content/AdminContent/z3mill/Gheybat.png" style="height:25px;width:25px;" /></td>
                        }
                        <td class="center">@item.Tarikh</td>
                        <td class="center">@item.Tovzihat</td>
                        <td class="center">@item.Emtiaz</td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p style="text-align: center;font-size: 20px;color: #c80b0b;margin-top: 10px;border-top: 1px solid;padding: 20px;border-top-color: lightgray;">در حال حاضر هیچ تشویق یا تنبیهی برای دانش آموز در این ماه ثبت نشده است !</p>
    }
</div>